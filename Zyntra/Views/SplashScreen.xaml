<Window x:Class="Zyntra.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zyntra" Icon="/logo.ico"
        Height="320" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">

    <Border Background="{StaticResource MainBrush}" CornerRadius="14"
            BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Center content -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Logo -->
                <Image Source="/logo.ico" Width="64" Height="64" RenderOptions.BitmapScalingMode="HighQuality"
                       HorizontalAlignment="Center" Margin="0,0,0,16" />

                <!-- App name -->
                <TextBlock Text="Zyntra" FontSize="28" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" />

                <!-- Tagline -->
                <TextBlock Text="Your personal app launcher" FontSize="13"
                           Foreground="{StaticResource SubTextBrush}" HorizontalAlignment="Center"
                           Margin="0,4,0,0" />
            </StackPanel>

            <!-- Bottom loading area -->
            <StackPanel Grid.Row="1" Margin="30,0,30,24" HorizontalAlignment="Stretch">
                <!-- Status text -->
                <TextBlock x:Name="StatusText" Text="Initializing..." FontSize="11"
                           Foreground="{StaticResource SubTextBrush}" HorizontalAlignment="Center"
                           Margin="0,0,0,10" />

                <!-- Progress bar -->
                <Border CornerRadius="4" Background="{StaticResource ControlBrush}" Height="6">
                    <Border x:Name="ProgressFill" CornerRadius="4" Background="{StaticResource AccentBrush}"
                            HorizontalAlignment="Left" Width="0" Height="6" />
                </Border>

                <!-- Version -->
                <TextBlock Text="v1.0.0" FontSize="10" Foreground="{StaticResource SubTextBrush}"
                           HorizontalAlignment="Center" Margin="0,10,0,0" Opacity="0.5" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
