<UserControl x:Class="Zyntra.Views.SteamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Zyntra.ViewModels">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Steam" FontSize="22" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}" />
                <TextBlock Text="Quick-switch Steam accounts and launch games" FontSize="13"
                           Foreground="{StaticResource SubTextBrush}" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="🎮 Launch Game" Padding="12,7" Margin="0,0,8,0"
                        Style="{StaticResource AccentButtonStyle}"
                        Command="{Binding LaunchGameCommand}" />
                <Button Content="↻ Refresh" Padding="12,7"
                        Style="{StaticResource ControlButtonStyle}"
                        Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <!-- Current User Info -->
        <Border Grid.Row="2" Background="{StaticResource PanelInsetBrush}" CornerRadius="8"
                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Current Steam User" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" />
                    <TextBlock FontSize="13" Foreground="{StaticResource SubTextBrush}" Margin="0,4,0,0"
                               Text="{Binding CurrentUser, FallbackValue='None', TargetNullValue='None'}" />
                </StackPanel>
                <Button Grid.Column="1" Content="🔄 Restart Steam" Padding="12,7"
                        Style="{StaticResource ControlButtonStyle}" VerticalAlignment="Center"
                        Command="{Binding RestartSteamCommand}" />
            </Grid>
        </Border>

        <!-- Not installed message -->
        <TextBlock Grid.Row="4" Text="Steam is not installed or could not be detected."
                   Foreground="{StaticResource SubTextBrush}" FontSize="14"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding SteamInstalled, Converter={StaticResource InverseBoolToVisConverter}}" />

        <!-- Account List -->
        <ListBox Grid.Row="4" ItemsSource="{Binding KnownUsers}"
                 SelectedItem="{Binding SelectedUser}"
                 Style="{StaticResource DarkListBoxStyle}"
                 ItemContainerStyle="{StaticResource DarkListBoxItemStyle}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch"
                 Visibility="{Binding SteamInstalled, Converter={StaticResource BoolToVisConverter}}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Steam icon -->
                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18"
                                Background="{StaticResource ControlHoverBrush}" VerticalAlignment="Center">
                            <TextBlock Text="🎮" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <!-- Username -->
                        <TextBlock Grid.Column="1" Text="{Binding}" FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="12,0" />

                        <!-- Switch button -->
                        <Button Grid.Column="2" Content="Switch" Padding="14,7"
                                Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center"
                                Command="{Binding DataContext.SwitchAccountCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status Bar -->
        <Border Grid.Row="5" Margin="0,12,0,0" Padding="0,8,0,0"
                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource SubTextBrush}" FontSize="12" />
        </Border>
    </Grid>
</UserControl>
