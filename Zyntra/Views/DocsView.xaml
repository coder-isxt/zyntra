<UserControl x:Class="Zyntra.Views.DocsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Zyntra.ViewModels">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Documentation" FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}" />
            <TextBlock Text="Lua scripting API reference and plugin development guide" FontSize="13"
                       Foreground="{StaticResource SubTextBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Tab Bar -->
        <Border Grid.Row="2" Background="{StaticResource PanelInsetBrush}" CornerRadius="8"
                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" Padding="4">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnOverview" Content="Overview" Padding="16,7"
                        Style="{StaticResource SidebarActiveButtonStyle}"
                        Click="OnDocNav" Tag="overview" FontSize="12" />
                <Button x:Name="BtnLuaApi" Content="Lua API Reference" Padding="16,7"
                        Style="{StaticResource SidebarButtonStyle}"
                        Click="OnDocNav" Tag="lua" FontSize="12" />
                <Button x:Name="BtnScriptUI" Content="Script UI API" Padding="16,7"
                        Style="{StaticResource SidebarButtonStyle}"
                        Click="OnDocNav" Tag="scriptui" FontSize="12" />
            </StackPanel>
        </Border>

        <!-- Content -->
        <Border Grid.Row="4" Background="{StaticResource PanelInsetBrush}" CornerRadius="8"
                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled" Padding="0">
                <StackPanel x:Name="DocPanel" Margin="28,24,28,28" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
