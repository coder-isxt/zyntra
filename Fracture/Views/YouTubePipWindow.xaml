<Window x:Class="Fracture.Views.YouTubePipWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="YouTube PiP" Height="260" Width="420"
        MinHeight="180" MinWidth="300"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Topmost="True"
        Background="Transparent" ResizeMode="CanResizeWithGrip">

    <Border Background="{StaticResource MainBrush}" CornerRadius="12"
            BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1"
            Effect="{StaticResource PopupShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource TopBrush}"
                    CornerRadius="12,12,0,0" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="YouTube PiP" Foreground="{StaticResource TextBrush}"
                               FontSize="12" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="12,0,0,0" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="_" Style="{StaticResource TitleBarButtonStyle}"
                                Width="38" Height="30" Click="MinimizeClick" />
                        <Button Content="x" Style="{StaticResource CloseButtonStyle}"
                                Width="38" Height="30" Click="CloseClick" />
                    </StackPanel>
                </Grid>
            </Border>

            <wv2:WebView2 x:Name="PipWebView" Grid.Row="1"
                          DefaultBackgroundColor="#0D1117" />
        </Grid>
    </Border>
</Window>
