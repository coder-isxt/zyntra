<UserControl x:Class="Fracture.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,28,32,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Text="Startup Manager" Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="Disable programs that launch at boot to start Windows faster. Disabled items are kept and can be re-enabled anytime."
                           Style="{StaticResource PageSubtitleStyle}" />
            </StackPanel>
            <Button Content="Refresh" Style="{StaticResource ControlButtonStyle}" Padding="16,8"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Command="{Binding RefreshCommand}" />
        </Grid>

        <!-- Startup list -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="8">
            <Grid>
                <!-- Empty state -->
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding Entries.Count, Converter={StaticResource ZeroToVisConverter}}">
                    <TextBlock Text="🚀" FontSize="40" HorizontalAlignment="Center" Opacity="0.5" />
                    <TextBlock Text="No startup items found" FontSize="15" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" Margin="0,10,0,0" />
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Entries}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="14,12" Margin="2,2"
                                        Background="{StaticResource CardBrush}" CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextBrush}" />
                                                <Border CornerRadius="4" Padding="6,1" Margin="8,0,0,0"
                                                        Background="{StaticResource AccentSoftBrush}" VerticalAlignment="Center"
                                                        Visibility="{Binding ShowAdminBadge, Converter={StaticResource BoolToVisConverter}}">
                                                    <TextBlock Text="ADMIN" FontSize="9" FontWeight="Bold"
                                                               Foreground="{StaticResource AccentBrush}" />
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding LocationText}" FontSize="11"
                                                       Foreground="{StaticResource SubTextBrush}" Margin="0,2,0,0" />
                                            <TextBlock Text="{Binding Command}" FontSize="10.5"
                                                       Foreground="{StaticResource SubTextBrush}" Opacity="0.6"
                                                       Margin="0,2,0,0" TextTrimming="CharacterEllipsis" MaxWidth="620"
                                                       HorizontalAlignment="Left" />
                                        </StackPanel>
                                        <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"
                                                  VerticalAlignment="Center"
                                                  IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Foreground="{StaticResource SubTextBrush}"
                   FontSize="12" Margin="0,12,0,14" />
    </Grid>
</UserControl>
